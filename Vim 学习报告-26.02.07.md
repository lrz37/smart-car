# Vim 学习报告

## 1. 概述
Vim（**Vi IMproved**）是一款高度可配置的纯文本编辑器，旨在实现极高效的文本编辑。它并非传统意义上的“编程语言”，而是一种通过纯键盘指令操作的“编辑语言”。其核心哲学是：双手不离主键盘区，通过模式切换实现光标移动、文本修改和系统级操作。

## 2. 核心工作模式 
Vim 的状态机设计是其区别于普通编辑器的根本，相同的按键在不同模式下有完全不同的功能。

* **普通模式 (Normal Mode)**：默认模式。按键被解释为操作指令（如移动、删除、复制）。
    * *进入方式*：在任何模式下按 `Esc`。
* **插入模式 (Insert Mode)**：文本输入模式，与普通记事本无异。
    * *进入方式*：`i` (当前光标前插入), `a` (当前光标后附加), `o` (下方新开一行), `O` (上方新开一行)。
* **可视模式 (Visual Mode)**：文本选择模式，用于高亮区块后执行批量操作。
    * *进入方式*：`v` (字符级选择), `V` (行级选择), `Ctrl+v` (块级/列级选择，对批量注释代码特别有用)。
* **命令行模式 (Command-line Mode)**：执行全局系统命令。
    * *进入方式*：在普通模式下输入 `:`。

## 3. 指令集

### 3.1 导航与移动
放弃方向键，使用主键盘区进行精准定位。
* **基础方向**：`h` (左), `j` (下), `k` (上), `l` (右)。
* **词级跳跃**：`w` (跳至下个词首), `b` (跳至上个词首), `e` (跳至当前或下个词尾)。
* **行级跳跃**：`0` (绝对行首), `^` (行首第一个非空白字符), `$` (绝对行尾)。
* **文件级跳跃**：`gg` (文件第一行), `G` (文件最后一行), `50G` (精确跳转到第50行)。

### 3.2 文本操作语法：操作符 + 动作
Vim 的编辑指令可以像自然语言一样组合：**操作符 (执行什么动作) + 文本对象 (作用于什么范围)**。
* **常用操作符**：`d` (删除/剪切), `y` (复制/Yank), `c` (修改/Change，即删除并直接进入插入模式)。
* **实战组合**：
    * `dw`：删除光标到下一个单词开头的字符 (Delete Word)。
    * `cw`：删除当前单词并进入插入模式 (Change Word)。
    * `yy`：复制当前整行。
    * `dd`：剪切当前整行。
    * `p`：在光标之后/下方粘贴 (Put)。

### 3.3 文本对象
在处理代码或有结构的文本时效率极高。格式通常为 `i` (inner, 不包含边界符号) 或 `a` (around, 包含边界符号)。
* `ci"`：修改双引号内的内容（自动清空引号内的字符串并进入插入模式）。
* `di(`：删除小括号内的所有内容（常用于修改函数参数）。
* `vi{`：选中大括号 `{}` 内的所有代码块。

## 4. 终端环境下的实用命令
在通过 SSH 远程连接服务器修改配置文件或脚本时，以下命令是刚需：
* **全局搜索与替换**：`:%s/旧文本/新文本/g` (将全文的旧文本替换为新文本)。
* **快速缩进**：普通模式下按 `>>` 将当前行向右缩进，`<<` 向左缩进；选中多行后按 `>` 或 `<` 进行批量代码缩进。
* **撤销与重做**：`u` (撤销操作), `Ctrl+r` (重做被撤销的操作)。
* **保存与退出**：`:wq` 或 `:x` (保存并退出), `:q!` (强制退出，彻底丢弃未保存的修改)。
