# 26.01.21工作总结——编码器与陀螺仪

---

## 一、 编码器 (Encoder)

### 1. 功能定义
编码器主要安装在电机输出轴或轮轴上，用于测量**轮子**的旋转状态。它是系统感知“自己走了多远”和“轮子转多快”的直接来源。

### 2. 工作原理
* **物理计数**
：通过光电或磁感应原理，将旋转机械位移转换为一系列数字脉冲信号。
* **方向识别**
：利用 A、B 两路相位差信号，单片机可以精准判断电机的正转与反转。
* **数据反馈**
：单片机通过计算单位时间内收到的脉冲数量，换算出当前的物理速度。

### 3. 核心局限：打滑效应
编码器只能感知轮子相对于电机的运动。当车辆处于冰面、沙地或悬空状态时，轮子剧烈旋转（空转）但车身并未移动，此时编码器反馈的位移数据是虚假的。

解决办法：负压吊扇

---

## 二、 陀螺仪与加速度计 (IMU)

### 1. 功能定义
陀螺仪与加速度计通常集成在 IMU 模块中，安装在车体中心。它监测的是
**车身整体**
在空间中的平衡和指向，不依赖于轮子的转动。

### 2. 物理特性
* **陀螺仪**：测量**角速度**
。它对旋转非常敏感，用于检测车身是否倾斜、是否正在转弯。
* **加速度计**：测量**线加速度**
（包括重力）。在静止或匀速状态下，它能通过感应重力方向来确定车身的绝对水平倾角。

### 3. 核心局限：时间偏移（漂移）
陀螺仪存在物理上的“零点漂移”。即使车身完全静止，传感器输出的数据也会有极小的跳动。由于角度是由角速度随时间累加算出的，这些微小误差会随着时间推移不断放大，导致角度计算逐渐偏离真实值。

***

## 三、 编码器与陀螺仪的 PCB 设计重点

### 陀螺仪

- 陀螺仪内部是极其微小的机械结构（MEMS），电路板的变形、震动和电源噪声都会严重影响其精度。
- **布局定位 (Placement)**
  • 中心原则：IMU 芯片应尽可能放置在 PCB 的几何中心或车辆的旋转轴中心。偏离中心会产生额外的向心加速度补偿需求。
  • 远离热源：远离大功率电阻、MOSFET 和 LDO 稳压器。温度波动会导致陀螺仪产生“温漂”（静止时数值缓慢变化）。
  • 远离震动源：不要紧贴电机接口或机械支撑柱，这些位置的机械应力最高。
- **机械应力防护**
  • 禁止过孔：在 IMU 芯片正下方的所有层，尽量不要放置任何过孔。过孔在焊接和受热时产生的应力会传递给芯片。
  • 地平面完整：芯片下方应保持完整的一块地平面，不要有走线穿过，起到屏蔽电磁干扰的作用。
  • 槽孔隔离：在某些高精度场合，会在 IMU 芯片周围的 PCB 上开“U型槽”，物理阻断板材变形带来的应力。
- **电源滤波**
  • 独立 LDO：如果条件允许，给 IMU 单独提供一路 LDO 供电，防止电机驱动产生的纹波进入传感器。
  • 电容放置：去耦电容必须紧贴芯片引脚。

### 编码器

编码器传输的是高速脉冲信号，最怕的是由于“走线太长”或“靠大电流太近”导致**的丢步或毛刺。**

**接口防静电与滤波**

- TVS 保护：编码器通常通过排线连接，容易受到人体静电攻击。在 PCB 接口处必须放置 TVS 管。
- RC 滤波：在进入单片机引脚前，串联一个小电阻（如 100 欧）并并联一个电容（如 100pF-1nF），可以有效滤除线缆吸收的高频干扰。

**布线防干扰 **

- 远离功率线：编码器的 A/B 相走线严禁靠近 XT60 电源输入线、电机输出线（PWM 线）。
- 包地处理：如果走线较长，建议对 A/B 相信号进行“包地”处理（在信号线两侧各拉一条地线），减少串扰。
- 等长走线：虽然编码器频率不算极高，但尽量保证 A、B 两根线长度相近，以维持稳定的相位差。

---

## 四、 智能车实际应用

在智能车应用中，两者是**互补关系**而非替代关系：
* **编码器**
在平整路面和稳定行驶时提供精准的速度参考。
* **陀螺仪**
在路面颠簸、轮胎打滑或需要维持平衡时提供可靠的姿态补偿。
* **综合策略**
通常利用加速度计的长期稳定性来校准陀螺仪的短期漂移，同时利用陀螺仪的灵敏度来弥补编码器在打滑瞬间的感知盲区。

---