# 26.01.22工作总结

今日学习：逻辑电路——74HC14 反相器、 74LS08 与门逻辑

***

## 74HC14 反相器

![image-20260122105752594](C:\Users\zhenq\AppData\Roaming\Typora\typora-user-images\image-20260122105752594.png)

74HC14 是一款包含六路独立**施密特触发反相器**的集成电路。

- **反相功能**：当输入为高电平时，输出为低电平；反之亦然。
- **施密特触发特性**：它具有滞回电压特性，能够将缓慢变化或带有波纹的输入信号整形为边缘陡峭、清晰的数字方波。

#### 原理图实际应用

- 核心板中输出的指示前进方向**两路GPIO信号**，输入74HC14反相器中，输出**反向的清晰方波信号**INVERT1、2
- 由于电机驱动环境（如 H 桥切换）存在严重的**电磁干扰**，MCU 输出的 PWM 或控制信号可能产生**毛刺**。74HC14 的施密特特性可以**过滤这些微小波动**，确保发送给驱动芯片（如 IR2184）的逻辑始终是明确的“0”或“1”。
- 如果 MCU 使用 3.3V 逻辑，而驱动芯片（如 IR2184）需要更高的输入电平或更强的驱动电流，74HC14 可以起到**电平缓冲和提升驱动能力**的作用。
- 反相器充当了 MCU 和功率驱动电路之间的“防波堤”。如果后级驱动电路发生击穿高压回流，74HC14 会作为第一道屏障损坏，从而**降低昂贵的 MCU 被烧毁的风险。**
- 在半桥驱动（如 IR2184）中，通常需要互补的逻辑信号。通过反相器，可以用一路信号同时获得正向和反向两种状态，**简化软件控制逻辑**。



***

## 74LS08四路双输出与门

该芯片内部包含四个独立的“与”门。其逻辑特性是：**只有当两个输入端（A 和 B）同时为高电平时，输出端（Y）才会输出高电平**。

![image-20260122152334346](C:\Users\zhenq\AppData\Roaming\Typora\typora-user-images\image-20260122152334346.png)

### 电流通道逻辑拆解

电路将控制逻辑分为两组，分别对应两个电机（MOTOR1 和 MOTOR2）：

####  **电机 1 控制逻辑（左侧引脚 1-6）**

- **正转通道 (`IN1_A`)**：
  - **输入**：第 4 脚接 `MOTOR1_PWM`（速度），第 5 脚接 `DIRECTION_1_GPIO`（方向）。
  - **逻辑**：当方向引脚为高且 PWM 有脉冲时，`IN1_A` 输出 PWM 信号。
- **反转通道 (`IN1_B`)**：
  - **输入**：第 1 脚接 `MOTOR1_PWM`（速度），第 2 脚接 `INVERT_1`（由 74HC14 取反后的方向信号）。
  - **逻辑**：当方向引脚为低（此时 `INVERT_1` 为高）且 PWM 有脉冲时，`IN1_B` 输出 PWM 信号。

####  **电机 2 控制逻辑（右侧引脚 8-13）**

- **正转通道 (`IN2_A`)**：由第 9 脚 (`MOTOR2_PWM`) 和 第 8 脚 (`IN2_A` 的输出反馈或使能，图中连接逻辑与电机 1 对称，由第 13 脚 `DIRECTION_2_GPIO` 控制逻辑分布) 组合。
- **反转通道 (`IN2_B`)**：由第 12 脚 (`MOTOR2_PWM`) 和 第 10 脚 (`INVERT_2`) 组合。

####  **通过这种硬件组合，可以通过简单的引脚控制实现复杂的电机动作：**	

| **状态** | **PWM 输入** | **方向引脚 (DIR)** | **反相信号 (INVERT)** | **IN1_A 输出** | **IN1_B 输出** |
| -------- | ------------ | ------------------ | --------------------- | -------------- | -------------- |
| **停止** | 低 (0)       | 任意               | 任意                  | **0**          | **0**          |
| **正转** | **脉冲**     | 高 (1)             | 低 (0)                | **PWM 脉冲**   | 0              |
| **反转** | **脉冲**     | 低 (0)             | 高 (1)                | 0              | **PWM 脉冲**   |

### 采用该芯片的优势

- **防止“直通”短路**：由于 `DIRECTION` 和 `INVERT` 信号在逻辑上永远相反，74LS08 从硬件层面保证了 `IN1_A` 和 `IN1_B` 绝不会同时有效。这有效保护了后级的 H 桥驱动芯片，防止因逻辑错误导致上下桥臂同时导通（烧毁电源）。
- **信号整形与驱动**：74LS08 充当了单片机与大电流驱动芯片之间的缓冲层，增加了信号的驱动能力，并保护单片机引脚不受反向电动势的干扰。
- **节省单片机资源**：控制一个电机只需占用一个硬件 PWM 通道和一个普通 GPIO，无需复杂的软件互补 PWM 配置。软件只需要产生一路连续的 PWM 信号和一路简单的高低电平方向信号，即可完成复杂的电机可逆调速控制。

