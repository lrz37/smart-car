# MobaXterm 终端连接与调试工作报告

## 1. 概述
本报告总结了近期使用 MobaXterm 作为核心终端工具，进行系统级远程访问、底层硬件调试以及跨平台文件交互的工作进展。MobaXterm 集成了丰富的网络工具和终端仿真功能，大幅提升了多节点管理和嵌入式开发环境下的联调效率。

## 2. 远程系统连接与管理
为实现对 Linux 服务器及开发主板的无缝接管，建立了稳定的 SSH 远程连接：
* **会话配置 (Session Setup)**：创建并保存了标准 SSH 会话，配置了目标 Linux 系统的 IP 地址、端口（默认 22）及认证凭据，实现了快速的一键接入。
* **环境初始化**：成功连接后，验证了基础环境变量（如 `PATH`）的加载情况，确保交叉编译工具链和核心编辑器（如 Vim）能够在远程会话中被正常调用。
* **连接稳定性监控**：启用了 MobaXterm 的 `SSH Keepalive` 功能，有效避免了因网络波动或长时间无操作导致的终端连接假死或中断。

## 3. 硬件调试与串口通信
针对底层硬件开发与主板级测试，利用 MobaXterm 的 Serial 功能进行了直接的物理层通信：
* **串口接入**：通过 USB 转串口模块连接目标硬件，在 MobaXterm 中新建 Serial 会话，并精准匹配了目标设备的 COM 端口号。
* **波特率与流控配置**：根据硬件数据手册（Datasheet），配置了正确的波特率（如 115200 或 9600）、数据位、停止位及奇偶校验，关闭了不必要的硬件流控。
* **指令下发与日志抓取**：通过串口终端成功向主板下发了底层调试指令，实时监控并抓取了设备启动过程中的 Bootloader 及内核打印日志，为排查硬件驱动状态和系统启动异常提供了关键数据。

## 4. 跨平台文件传输 
在代码编写与目标板部署之间，利用 MobaXterm 内置的图形化 SFTP 面板实现了高效的数据流转：
* **可视化文件管理**：通过终端左侧的 SFTP 侧边栏，直接浏览远程 Linux 系统的目录结构，无需额外挂载或输入繁琐的 `scp` 命令。
* **快速双向同步**：
    * 将宿主机上编译生成的二进制可执行文件（如交叉编译产物）快速拖拽上传至目标开发板的指定目录。
    * 将远程系统运行生成的测试数据、日志文件或需要分析的配置文件直接拖拽下载至本地工作区。
* **权限管理**：在传输完成后，直接通过右键菜单或结合终端命令（`chmod`）快速调整了上传文件的可执行权限。

