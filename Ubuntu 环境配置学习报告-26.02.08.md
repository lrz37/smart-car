# Ubuntu 环境配置学习报告

## 1. 概述

本报告总结了Ubuntu 开发环境初始化与核心工具链配置工作。本次环境搭建的主要目标是为后续的底层硬件交叉编译提供稳定、高效的系统级支撑。

## 2. 基础系统配置与远程访问

为了保障开发流程的顺畅与安全性，首先对裸机/虚拟机环境进行了基础网络和权限调优：

- **软件源更新与系统升级**：将 Ubuntu 默认的 APT 镜像源切换为国内高速镜像（清华源），并执行 `apt update` 与 `apt upgrade` 完成全系统依赖的更新。
- **SSH 服务部署**：安装 `openssh-server`，配置静态 IP 与防火墙规则，开放 22 端口，以支持通过 MobaXterm 等终端工具进行稳定的 SSH 远程连接、串行端口调试以及 SFTP 协议的文件传输。
- **基础网络排错工具**：安装了 `net-tools`、`curl` 等网络调试工具，确保后续开发中网络通信的顺畅。

## 3. 核心开发工具与编辑器配置

为了提升在无图形界面（CLI）环境下的代码编写与系统管理效率：

- **终端编辑器搭建**：安装并配置了 Vim 作为核心的文本编辑器。完成了一系列基础配置（如语法高亮、自动缩进、显示行号等），以满足快速修改系统配置文件和代码调试的需求。
- **版本控制系统**：安装 Git 并配置了全局用户名与邮箱，生成了 SSH Key 用于免密拉取和推送远程代码仓库。

## 4. 项目特定环境搭建

针对当前主要推进的各项工程任务，分别配置了相互隔离且功能完备的开发环境：

### 4.1 嵌入式系统与交叉编译环境

为支持硬件主板及内核级别的开发，搭建了完整的交叉编译平台：

- **工具链部署**：成功下载并配置了针对龙芯（LS2K0300）处理器的交叉编译工具链。
- **环境变量设置**：将交叉编译器的路径注入到系统环境（`~/.bashrc`）中，确保 `make` 及相关构建脚本能够正确调用目标架构的编译器。
- **依赖库安装**：安装了编译 Linux 内核所需的基础依赖，包括 `build-essential`、`bison`、`flex`、`libncurses-dev` 等，为后续的内核裁剪和应用程序编译做好了底层准备。

### 4.2 算法与深度学习环境

为支持数据处理与神经网络构建，部署了 GPU 加速运算环境：

- **Python 环境隔离**：安装了 Miniconda/Anaconda，并创建了独立的 Python 虚拟环境，避免不同项目间的包依赖冲突。
- **基础科学计算包**：在虚拟环境中部署了 NumPy、Pandas 等核心数据处理与分析工具库。
- **深度学习框架**：根据系统搭载的显卡型号，安装了对应版本的 CUDA Toolkit 与 cuDNN，并成功部署了 PyTorch 框架，验证了 GPU 张量运算的可用性。

## 5. 待办事项与后续计划

- 编写并固化一套一键配置环境的 Shell 脚本，以便未来在其他机器上快速复现当前的 Ubuntu 开发环境。
- 进一步优化 Vim 的配置文件（`.vimrc`），按需引入代码补全等高级插件。
- 在龙芯交叉编译环境中，启动首个“Hello World”级别应用程序的编译与板级运行测试，排查潜在的网络或构建脚本问题。